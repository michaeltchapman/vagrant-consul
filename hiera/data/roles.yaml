build_classes:
  - openstack_extras::repo::redhat::redhat
  - partial
  - partial::rpm
  - firewall

server_classes:
  - openstack_extras::repo::redhat::redhat
  - profile::application::consul
  - firewall

agent_classes:
  - openstack_extras::repo::redhat::redhat
  - profile::application::consul
  - firewall
